#!/bin/bash



function calicoctl {
  kubectl get pod calicoctl -n kube-system
  if [[ $? != 0 ]]; then
    kubectl apply -f https://docs.projectcalico.org/manifests/calicoctl-etcd.yaml
  fi
  kubectl exec -i -n kube-system calicoctl -- /calicoctl $@
}
